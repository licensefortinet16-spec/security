global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: container-security
    metrics_path: /metrics
    static_configs:
      - targets:
          - 192.168.1.22:8090
        labels:
          instance: security-status
          service: container-security-monitor
